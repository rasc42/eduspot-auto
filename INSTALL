This script is run by Python 3 and use _requests_ and _BeautifulSoup_ librairies.

To install the needed librairies on a *NIX system :

```
    $ pip install requests
```

```
    $ pip install beautifulsoup4
```
